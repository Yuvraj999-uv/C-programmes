#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int x,i,factorial=1;
    scanf("%d",&x);
    for(i=2;i<=x;i++){
        factorial= factorial*i;
    }
    printf("%d",factorial);
    return 0;
}
