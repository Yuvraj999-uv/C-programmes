#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int x,i,a=0,b=1,c ;
    scanf("%d",&x);
    if(x==0||x==1){
        printf("0");
    }
   else{
       printf("%d %d ",a,b);
   } 
    for(i=3;i<=x;i++){
        c=a+b;
           a=b ;
        b=c;
               
         printf("%d ",c)   ;
    }

    return 0;
}
