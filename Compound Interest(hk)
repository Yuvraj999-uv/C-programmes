#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
float p,r,t,a,ci;
    scanf("%f%f%f",&p,&r,&t);
    a=p*pow((100+r)/100,t);
    ci=a-p;
    printf("%.2f",ci);
    
   
    return 0;
}
